import React, { Fragment } from "react"
import styled from "styled-components"
import Autocomplete from "./autocomplete"

const InputAutocomplete = ({ values }) => (
  <Fragment>
    <Autocomplete values={values} />
  </Fragment>
)
